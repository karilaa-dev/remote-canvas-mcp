{
  "name": "remote-canvas-mcp",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --var SOURCE_COMMIT:$(git rev-parse --short HEAD) --tag $(git rev-parse --short HEAD)",
    "cf-typegen": "wrangler types",
    "test": "node --import tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit",
    "sync": "tsx scripts/sync-upstream.ts"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.3.0",
    "agents": "^0.7.6",
    "hono": "^4.12.7",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.73.0"
  }
}
